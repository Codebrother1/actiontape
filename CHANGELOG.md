# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

## [0.1.0] - unreleased

### Added

- Transparent MCP stdio recording to JSONL tapes (`record`) and read-only
  normalization/inspection (`inspect`)
- Deterministic YAML/JSON contract evaluation over recorded actions (`check`)
- Historical `tools/list` mapping-provenance analysis (`authzen plan`)
- Declared COAZ-MCP `x-authzen-mapping` renderer (`authzen render`)
- AuthZEN historical policy simulation (`authzen simulate`, `authzen
export`) and evidence-aware audit (`authzen audit`) with native Access
  Evaluations batch support and single-evaluation fallback
- `examples/cerbos-demo` — real-PDP (Cerbos 0.55.0) historical audit example
